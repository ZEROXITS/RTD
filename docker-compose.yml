version: '3.8'

services:
  rtd:
    build: .
    container_name: rtd-agent
    volumes:
      - ./config:/app/config
      - ./workspace:/app/workspace
    ports:
      - "7860:7860"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
