<p align="center">
  <img src="assets/logo.jpg" width="200"/>
</p>

العربية | [English](README.md) | [中文](README_zh.md) | [한국어](README_ko.md) | [日本語](README_ja.md)

[![GitHub stars](https://img.shields.io/github/stars/ZEROXITS/RTD?style=social)](https://github.com/ZEROXITS/RTD/stargazers)
&ensp;
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT) &ensp;
[![Demo](https://img.shields.io/badge/Demo-Hugging%20Face-yellow)](https://huggingface.co/spaces/lyh-917/OpenManusDemo)

# 👋 RTD: مستقبل وكلاء الذكاء الاصطناعي العام (General AI Agents)

**RTD** هو إطار عمل قوي ومفتوح المصدر لوكلاء الذكاء الاصطناعي العام، مصمم ليكون تنافسياً، قابلاً للتخصيص، وفعالاً للغاية. تم بناؤه على أسس OpenManus، ويهدف RTD إلى دفع حدود ما يمكن لوكلاء الذكاء الاصطناعي تحقيقه.

## الميزات الأساسية والمحسّنة

*   **وكيل ذكاء اصطناعي عام:** قادر على حل مجموعة واسعة من المهام باستخدام أدوات متعددة.
*   **نظام ذاكرة متقدم (جديد):** تم دمج نظام ذاكرة طويلة المدى يعتمد على التلخيص التلقائي، مما يسمح للوكيل بتذكر سياقات المحادثات الطويلة والمهام المعقدة دون تجاوز حدود الرمز المميز (Token Limits).
*   **دعم بروتوكول سياق النموذج (MCP):** يتيح التكامل السلس مع أدوات وخدمات خارجية.
*   **بيئة صندوق رمل آمنة:** تنفيذ الكود والعمليات في بيئة معزولة لضمان الأمان.
*   **وكلاء متخصصون:** دعم لوكلاء متخصصين مثل وكيل تحليل البيانات (DataAnalysis Agent).

## التجربة العملية

<video src="assets/demo.mp4" controls="controls" muted="muted" class="d-block rounded-bottom-2 border-top width-fit" style="max-height:640px; min-height: 200px"></video>

## التثبيت

نوصي باستخدام `uv` لسرعة التثبيت وإدارة التبعيات.

### الطريقة 1: استخدام `uv` (موصى به)

1.  **تثبيت `uv`:**
    ```bash
    curl -LsSf https://astral.sh/uv/install.sh | sh
    ```
2.  **استنساخ المستودع:**
    ```bash
    git clone https://github.com/ZEROXITS/RTD.git
    cd RTD
    ```
3.  **إنشاء بيئة افتراضية وتفعيلها:**
    ```bash
    /home/ubuntu/.local/bin/uv venv --python 3.11
    source .venv/bin/activate
    ```
4.  **تثبيت التبعيات:**
    ```bash
    /home/ubuntu/.local/bin/uv pip install -r requirements.txt
    ```

### أدوات إضافية (اختياري)

لتفعيل أتمتة المتصفح، قم بتثبيت متصفح `playwright`:
```bash
playwright install
```

## الإعدادات

يتطلب RTD إعداد مفاتيح API لنماذج اللغة الكبيرة (LLMs).

1.  **إنشاء ملف الإعدادات:**
    ```bash
    cp config/config.example.toml config/config.toml
    ```
2.  **تعديل `config/config.toml`:**
    أضف مفاتيح API الخاصة بك وقم بتخصيص الإعدادات:
    ```toml
    # إعدادات LLM العامة
    [llm]
    model = "gpt-4o"
    base_url = "https://api.openai.com/v1"
    api_key = "sk-..."  # استبدلها بمفتاح API الخاص بك
    max_tokens = 4096
    temperature = 0.0
    ```

## البدء السريع

لتشغيل وكيل RTD العام:

```bash
python main.py
```

يمكنك أيضاً تشغيل النسخة متعددة الوكلاء (Multi-Agent) غير المستقرة:

```bash
python run_flow.py
```

## المساهمة

نرحب بأي اقتراحات أو مساهمات! يمكنك فتح مشكلة (Issue) أو إرسال طلب سحب (Pull Request).

**ملاحظة:** قبل إرسال طلب سحب، يرجى التأكد من استخدام أداة `pre-commit` لفحص التغييرات: `pre-commit run --all-files`.

## الشكر والتقدير

نقدم الشكر لـ **FoundationAgents/OpenManus** على توفير الأساس لهذا المشروع.

## الترخيص

هذا المشروع مرخص بموجب ترخيص MIT.
